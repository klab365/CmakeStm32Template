#include "led.h"
#include "App.h"

/// @brief entry point of the application
void app_run()
{
    // while (true)
    // {
    //     led.toggle();
    // }

    while (true)
    {
        /* code */
    }
}

int calculate(int a, int b)
{
    return a + b;
}
